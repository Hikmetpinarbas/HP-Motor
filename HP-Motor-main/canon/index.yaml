version: "2026.1"
ssot_policy:
  single_source_of_truth: true
  engine_canon_is_primary: true
  legacy_canon_is_secondary: true

sources:
  primary_engine:
    root: "canon/engine"
    engine_registry_root: "canon/engine_registry"

  secondary_legacy_motor:
    root: "canon"
    files:
      - "canon/football_ontology.json"
      - "canon/metric_ontology.json"
      - "canon/platform_mappings.json"
      - "canon/registry.json"

contracts:
  canonical_pitch: "105x68"
  universal_signal_schema: "canon/engine/cdl/signal.schema.json"
  analysis_claim_schema: "canon/engine/cal/analysis_claim.schema.json"